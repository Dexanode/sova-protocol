# Optional override; the Whitechain Sepolia RPC is used by default.
WHITECHAIN_SEPOLIA_RPC_URL=https://rpc.testnet.whitechain.io

# Store the deployer key in Hardhat's encrypted keystore instead of this file:
# npx hardhat keystore set DEPLOYER_PRIVATE_KEY
